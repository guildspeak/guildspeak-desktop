import styled from '../../utils/styled-components'

const Wrapper = styled.div`
  display: flex;
  flex-direction: row;
  overflow: hidden;
  user-select: none;
  font-size: 16px;
  height: 72px;
  margin: -12px 0px 0px 0px;
  align-items: center;
`

export { Wrapper }
