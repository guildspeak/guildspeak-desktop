import styled from 'styled-components'

const Wrapper = styled.div`
  display: flex;
  flex-direction: row;
  overflow: hidden;
  user-select: none;
  font-size: 16px;
  padding: 2px 0px 5px 0px;
`

export { Wrapper }
